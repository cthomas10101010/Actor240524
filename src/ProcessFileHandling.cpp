#include "ProcessFileHandling.h"
#include <iostream>

void ProcessFileHandling::handleFileOperation(const std::string& filePath) {
    // Read the specified file, obtain handle, and process it
    std::cout << "Handling file: " << filePath << std::endl;
}
